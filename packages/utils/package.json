{
  "name": "@repo/utils",
  "version": "0.0.0",
  "private": true,
  "description": "Pure, environment-agnostic helpers — no DOM, no Node-only APIs",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "lint": "biome check .",
    "check-types": "tsc --noEmit",
    "test": "NODE_OPTIONS=--experimental-vm-modules jest --passWithNoTests"
  },
  "dependencies": {
    "@repo/schemas": "*"
  },
  "devDependencies": {
    "@repo/config": "*",
    "@types/jest": "29.5.14",
    "jest": "29.7.0",
    "ts-jest": "29.2.5",
    "typescript": "5.7.3"
  }
}
