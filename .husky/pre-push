#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh" 2>/dev/null || true

echo "▸ pre-push: lint, typecheck, test on affected workspaces"
npx turbo run lint check-types test --filter=...[HEAD~1] --concurrency=4
