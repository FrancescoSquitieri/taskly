import { CreateInviteSchema } from '@repo/schemas/invite';
import { CreateWorkspaceSchema } from '@repo/schemas/tenant';

export const createWorkspaceBodySchema = CreateWorkspaceSchema;
export const createInviteBodySchema = CreateInviteSchema;
